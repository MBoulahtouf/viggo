version: '3.8'

# All services moved to Azure
# Neo4j: Azure Container Instance at 20.216.195.227:7687
# Elasticsearch: Azure Cognitive Search at https://viggo-search.search.windows.net

services:
  # Placeholder service to keep docker-compose valid
  placeholder:
    image: hello-world
    command: echo "All services moved to Azure"
  # Neo4j moved to Azure Container Instance at 20.216.195.227:7687
  # neo4j:
  #   image: neo4j:5.15-community
  #   container_name: viggo-neo4j
  #   ports:
  #     - "7474:7474"  # HTTP
  #     - "7687:7687"  # Bolt
  #   environment:
  #     - NEO4J_AUTH=neo4j/[REDACTED]
  #     - NEO4J_PLUGINS=["apoc"]
  #     - NEO4J_dbms_security_procedures_unrestricted=apoc.*
  #     - NEO4J_dbms_security_procedures_allowlist=apoc.*
  #     - NEO4J_dbms_memory_heap_initial__size=512m
  #     - NEO4J_dbms_memory_heap_max__size=2G
  #     - NEO4J_dbms_memory_pagecache_size=1G
  #   volumes:
  #     - neo4j_data:/data
  #     - neo4j_logs:/logs
  #     - neo4j_import:/var/lib/neo4j/import
  #     - neo4j_plugins:/plugins
  #   restart: unless-stopped
  #   healthcheck:
  #     test: ["CMD", "cypher-shell", "-u", "neo4j", "-p", "[REDACTED]", "RETURN 1"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 5

# Elasticsearch moved to Azure Cognitive Search
  # elasticsearch:
  #   image: docker.elastic.co/elasticsearch/elasticsearch:8.11.0
  #   container_name: viggo-elasticsearch
  #   environment:
  #     - discovery.type=single-node
  #     - xpack.security.enabled=false
  #     - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
  #   ports:
  #     - "9200:9200"
  #     - "9300:9300"
  #   volumes:
  #     - elasticsearch_data:/usr/share/elasticsearch/data
  #   restart: unless-stopped
  #   healthcheck:
  #     test: ["CMD-SHELL", "curl -f http://localhost:9200/_cluster/health || exit 1"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 5

volumes:
  # All services moved to Azure
  # neo4j_data:
  # neo4j_logs:
  # neo4j_import:
  # neo4j_plugins:
  # elasticsearch_data:  # Moved to Azure Cognitive Search




